{
  "name": "poll-form-builder",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt build && nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "npm run generate && npx push-dir --dir=.output/public --branch=gh-pages --cleanup"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.11.0",
    "nuxt": "^3.10.0",
    "push-dir": "^0.4.1",
    "typescript": "^5.3.3",
    "vue": "^3.4.5",
    "vue-router": "^4.2.5"
  },
  "dependencies": {
    "@headlessui/vue": "^1.7.16",
    "@heroicons/vue": "^2.1.1",
    "@pinia/nuxt": "^0.5.1",
    "pinia": "^2.1.7"
  }
}